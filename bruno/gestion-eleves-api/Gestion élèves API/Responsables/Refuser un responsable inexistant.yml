info:
  name: Refuser un responsable inexistant
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/responsables/999999"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
