info:
  name: Lister les responsabilités légales
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/responsabilites-legales"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
