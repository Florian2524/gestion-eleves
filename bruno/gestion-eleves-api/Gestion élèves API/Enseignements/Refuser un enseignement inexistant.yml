info:
  name: Refuser un enseignement inexistant
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/enseignements/999999"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5