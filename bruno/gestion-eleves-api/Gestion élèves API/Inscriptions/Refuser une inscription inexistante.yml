info:
  name: Refuser une inscription inexistante
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/inscriptions/999999"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
