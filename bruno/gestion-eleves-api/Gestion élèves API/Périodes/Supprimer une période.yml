info:
  name: Supprimer une période
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/periodes/{{idPeriode}}"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
