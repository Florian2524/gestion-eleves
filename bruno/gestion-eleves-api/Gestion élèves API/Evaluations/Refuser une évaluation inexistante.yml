info:
  name: Refuser une évaluation inexistante
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/evaluations/999999"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
