info:
  name: Lister les évaluations
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/evaluations"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
