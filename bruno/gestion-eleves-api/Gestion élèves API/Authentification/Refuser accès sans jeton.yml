info:
  name: Refuser l'accès sans jeton
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/matieres"
auth: none

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "401"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
