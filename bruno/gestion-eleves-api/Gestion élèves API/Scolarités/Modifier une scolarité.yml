info:
  name: Modifier une scolarité
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/scolarites/{{idScolarite}}"
  body:
    type: json
    data: |-
      {
        "idEleve": {{idEleve}},
        "idClasse": {{idClasse}},
        "dateDebut": "2026-09-01",
        "dateFin": "2027-06-30",
        "statut": "TERMINEE"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
