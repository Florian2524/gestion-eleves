spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/gestion_eleves
    username: gestion_eleves
    password: gestion_eleves

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

application:
  security:
    jwt:
      secret: ${JWT_SECRET:Z2VzdGlvbi1lbGV2ZXMtZGV2ZWxvcG1lbnQtand0LXNlY3JldC0yMDI2}
      expiration-seconds: ${JWT_EXPIRATION_SECONDS:3600}

server:
  port: 8080
